@using MyPortfolio.Data
@model List<Education>
@{
    ViewData["Title"] = "Eğitim";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewData["ActiveMenu"] = "Education";

    int count = 0;
}

<div class="page-section">

    <div class="toolbar">
        <a asp-controller="AdminEducation" asp-action="CreateEducation" class="btn btn-add">
            <i class="fa-solid fa-plus"></i> Yeni Eğitim Ekle
        </a>
    </div>

    @if (TempData["Success"] != null)
    {
        <div style="margin-bottom:18px; padding:12px 16px; border-radius:12px;
                        border:1px solid rgba(34,197,94,0.35);
                        background: rgba(34,197,94,0.08);
                        color:#bbf7d0;">
            <i class="fa-solid fa-circle-check" style="margin-right:8px;"></i>
            @TempData["Success"]
        </div>
    }

    <div class="table-container">
        <table>
            <thead>
                <tr>
                    <th>#</th>
                    <th>Başlık</th>
                    <th>Okul</th>
                    <th>Yıl</th>
                    <th>Açıklama</th>
                    <th style="width:180px;">İşlemler</th>
                </tr>
            </thead>

            <tbody>
                @if (Model == null || !Model.Any())
                {
                    <tr>
                        <td colspan="6" style="text-align:center; padding:40px; color: var(--text-muted);">
                            <i class="fa-regular fa-folder-open" style="font-size:24px; margin-bottom:10px; display:block;"></i>
                            Henüz eğitim eklenmemiş.
                        </td>
                    </tr>
                }
                else
                {
                    foreach (var item in Model)
                    {
                        count++;
                        <tr>
                            <td>@count</td>
                            <td><strong style="color:#fff;">@item.Title</strong></td>
                            <td>@item.Name</td>
                            <td><span style="color: var(--accent-purple); font-weight:500;">@item.Year</span></td>
                            <td style="max-width:520px;">
                                @(string.IsNullOrWhiteSpace(item.Description) ? "-" : item.Description)
                            </td>
                            <td>
                                <div class="action-buttons">
                                    <a asp-controller="AdminEducation" asp-action="UpdateEducation" asp-route-id="@item.EducationId"
                                       class="btn btn-sm btn-edit">
                                        <i class="fa-solid fa-pen"></i> Güncelle
                                    </a>

                                    <a asp-controller="AdminEducation" asp-action="DeleteEducation" asp-route-id="@item.EducationId"
                                       class="btn btn-sm btn-delete"
                                       onclick="return confirm('Bu eğitimi silmek istediğinize emin misiniz?')">
                                        <i class="fa-solid fa-trash"></i> Sil
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>
