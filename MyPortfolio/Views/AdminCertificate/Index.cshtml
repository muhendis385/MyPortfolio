@using MyPortfolio.Data
@model List<Certificate>
@{
    ViewData["Title"] = "Sertifikalar";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewData["ActiveMenu"] = "Certificate";

    int count = 0;
}

<div class="page-section">

    <div class="toolbar">
        <a asp-controller="AdminCertificate" asp-action="CreateCertificate" class="btn btn-add">
            <i class="fa-solid fa-plus"></i> Yeni Sertifika Ekle
        </a>
    </div>

    @if (TempData["Success"] != null)
    {
        <div style="margin-bottom:18px; padding:12px 16px; border-radius:12px;
                        border:1px solid rgba(34,197,94,0.35);
                        background: rgba(34,197,94,0.08);
                        color:#bbf7d0;">
            <i class="fa-solid fa-circle-check" style="margin-right:8px;"></i>
            @TempData["Success"]
        </div>
    }

    <div class="table-container">
        <table>
            <thead>
                <tr>
                    <th>#</th>
                    <th>Sertifika Adı</th>
                    <th>Açıklama</th>
                    <th style="width:180px;">İşlemler</th>
                </tr>
            </thead>

            <tbody>
                @if (Model == null || !Model.Any())
                {
                    <tr>
                        <td colspan="4" style="text-align:center; padding:40px; color: var(--text-muted);">
                            <i class="fa-regular fa-folder-open" style="font-size:24px; margin-bottom:10px; display:block;"></i>
                            Henüz sertifika eklenmemiş.
                        </td>
                    </tr>
                }
                else
                {
                    foreach (var item in Model)
                    {
                        count++;
                        <tr>
                            <td>@count</td>
                            <td><strong style="color:#fff;">@item.Name</strong></td>
                            <td style="max-width:700px;">
                                @(string.IsNullOrWhiteSpace(item.Description) ? "-" : item.Description)
                            </td>
                            <td>
                                <div class="action-buttons">
                                    <a asp-controller="AdminCertificate" asp-action="UpdateCertificate" asp-route-id="@item.CertificateId"
                                       class="btn btn-sm btn-edit">
                                        <i class="fa-solid fa-pen"></i> Güncelle
                                    </a>

                                    <a asp-controller="AdminCertificate" asp-action="DeleteCertificate" asp-route-id="@item.CertificateId"
                                       class="btn btn-sm btn-delete"
                                       onclick="return confirm('Bu sertifikayı silmek istediğinize emin misiniz?')">
                                        <i class="fa-solid fa-trash"></i> Sil
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>
